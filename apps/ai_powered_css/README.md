# ai_powered_css (Frappe app)

Custom Frappe app that powers the support chat UI, chat API endpoints, and Helpdesk ticket escalation.
Core pieces live under `ai_powered_css/` (API handlers, DocTypes, and the `/support-chat` web page).
